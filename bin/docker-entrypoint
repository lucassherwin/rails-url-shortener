#!/bin/bash -e

# Install missing gems (fast no-op if up to date; runs on first boot or after Gemfile changes)
bundle check 2>/dev/null || bundle install

# Create and migrate the database (idempotent: no-op when already up to date)
./bin/rails db:prepare

exec "${@}"
